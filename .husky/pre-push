branch=$(git branch --show-current)

if [ "$branch" = "main" ]; then
  echo "🚫 Direct push to 'main' branch is not allowed!"
  echo "💡 Please create a pull request instead."
  exit 1
fi

echo "✅ Push allowed - not pushing to main branch"
